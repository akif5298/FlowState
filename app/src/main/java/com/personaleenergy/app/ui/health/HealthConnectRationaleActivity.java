package com.personaleenergy.app.ui.health;

import android.os.Bundle;
import androidx.annotation.Nullable;
import androidx.appcompat.app.AppCompatActivity;

public class HealthConnectRationaleActivity extends AppCompatActivity {
    @Override
    protected void onCreate(@Nullable Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        // This activity will be expanded later to show a rationale for health permissions.
        // For now, it will just finish.
        finish();
    }
}

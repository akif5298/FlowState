{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mvn clean package -DskipTests"
  },
  "deploy": {
    "startCommand": "echo 'Current directory:' && pwd && echo 'Listing target directory:' && ls -la target/ 2>/dev/null || echo 'target/ not found' && echo 'Looking for JAR files:' && find . -name '*.jar' 2>/dev/null && echo 'Trying to start JAR...' && cd target && java -jar hibp-service-1.0.0.jar",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}

